{
  "//Urls": "Bound to 0.0.0.0 so a phone or emulator on the same network can reach the dev API. Keeps `dotnet run --no-launch-profile` in agreement with launchSettings.json.",
  "Urls": "http://0.0.0.0:5000",
  "Logging": {
    "LogLevel": {
      "Default": "Debug",
      "Microsoft.AspNetCore": "Information"
    }
  },
  "MongoDb": {
    "ConnectionString": "mongodb://localhost:27017",
    "DatabaseName": "ride_journal_dev"
  },
  "Jwt": {
    "Secret": "DevSecretKey_AtLeast32Characters_Long!!",
    "Issuer": "RideJournal",
    "Audience": "RideJournalApp",
    "AccessTokenExpirationMinutes": 120,
    "RefreshTokenExpirationDays": 30
  }
}
