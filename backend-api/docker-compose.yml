# Development: MongoDB only — run API natively via `dotnet run`
services:
  mongodb:
    image: mongo:7
    container_name: drivejournal-mongodb
    ports:
      - "27017:27017"
    volumes:
      - mongodb_data:/data/db
    restart: unless-stopped

volumes:
  mongodb_data:
