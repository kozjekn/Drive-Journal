# Copy to .env (web / iOS simulator), .env.android (Android emulator), or
# .env.device (physical device on your LAN — use your machine IP).
# The dev API listens on port 5000; see backend-api/README.md.
API_BASE_URL=http://localhost:5000
GOOGLE_CLIENT_ID=
